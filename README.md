# Network Simulation in Go

This project is a network simulation written in Go. It simulates a network of routers and hosts, and demonstrates routing and package forwarding between them.

## How to Run

To run the simulation, use the following command:

`go run network.go`
